"""
Lambdata - a collection of ds helper functions
"""

import pandas as pd
import numpy as np

#CAP global variables
ONES = pd.DataFrame(np.ones(10))
ZEROS = pd.DataFrame(np.zeros(50)) #can put arbitrary code in these!